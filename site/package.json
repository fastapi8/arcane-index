{
  "name": "arcane-index-site",
  "version": "0.0.1",
  "license": "MIT",
  "scripts": {
    "data": "python3 ../research/scripts/export_site_data.py",
    "build": "npm run sources && evidence build",
    "build:strict": "npm run sources:strict && evidence build:strict",
    "dev": "npm run sources && evidence dev",
    "test": "evidence build",
    "sources": "npm run data && evidence sources",
    "sources:strict": "npm run data && evidence sources --strict",
    "preview": "evidence preview"
  },
  "engines": {
    "npm": ">=7.0.0",
    "node": ">=18.0.0"
  },
  "type": "module",
  "dependencies": {
    "@evidence-dev/core-components": "^5.4.2",
    "@evidence-dev/evidence": "^40.1.8",
    "@evidence-dev/sqlite": "^2.0.9",
    "katex": "^0.16.47"
  },
  "overrides": {
    "jsonwebtoken": "9.0.0",
    "trim@<0.0.3": ">0.0.3",
    "sqlite3": "5.1.5",
    "axios": "^1.7.4"
  }
}
